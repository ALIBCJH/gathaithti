/**
 * KISWAHILI — Our Coffee.
 *
 * Not yet translated. Copy the shape from ../en/products.ts and translate the
 * strings you need; anything left out falls back to English automatically.
 * Keep {{tokens}} exactly as they appear. See ./README.md.
 */
import type { ProductsContent, DeepPartial } from '../types';

export const products: DeepPartial<ProductsContent> = {
  // TODO(sw): translate this page.
};
