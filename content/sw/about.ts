/**
 * KISWAHILI — About.
 *
 * Not yet translated. Copy the shape from ../en/about.ts and translate the
 * strings you need; anything left out falls back to English automatically.
 * Keep {{tokens}} exactly as they appear. See ./README.md.
 */
import type { AboutContent, DeepPartial } from '../types';

export const about: DeepPartial<AboutContent> = {
  // TODO(sw): translate this page.
};
