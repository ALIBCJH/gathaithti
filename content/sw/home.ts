/**
 * KISWAHILI — Home.
 *
 * Not yet translated. Copy the shape from ../en/home.ts and translate the
 * strings you need; anything left out falls back to English automatically.
 * Keep {{tokens}} exactly as they appear. See ./README.md.
 */
import type { HomeContent, DeepPartial } from '../types';

export const home: DeepPartial<HomeContent> = {
  // TODO(sw): translate this page.
};
