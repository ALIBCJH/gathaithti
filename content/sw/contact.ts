/**
 * KISWAHILI — Contact.
 *
 * Not yet translated. Copy the shape from ../en/contact.ts and translate the
 * strings you need; anything left out falls back to English automatically.
 * Keep {{tokens}} exactly as they appear. See ./README.md.
 */
import type { ContactContent, DeepPartial } from '../types';

export const contact: DeepPartial<ContactContent> = {
  // TODO(sw): translate this page.
};
